<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".AwesomeA">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Notificationbar"
        android:id="@+id/notiButton"
        android:onClick="createNotification"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_marginLeft="32dp"
        android:layout_marginStart="32dp"
        android:layout_marginTop="35dp" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Toast"
        android:onClick="createToast"
        android:id="@+id/toastButton"
        android:layout_alignTop="@+id/notiButton"
        android:layout_toRightOf="@+id/notiButton"
        android:layout_toEndOf="@+id/notiButton"
        android:layout_marginLeft="45dp"
        android:layout_marginStart="45dp" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Popup Dialog"
        android:onClick="createPopup"
        android:id="@+id/popButton"
        android:layout_below="@+id/toastButton"
        android:layout_alignLeft="@+id/notiButton"
        android:layout_alignStart="@+id/notiButton"
        android:layout_marginTop="35dp" />

</RelativeLayout>
